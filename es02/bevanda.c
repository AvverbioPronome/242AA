#include <stdio.h>

int main(){
	float voto;
	
	scanf("%d", &voto);
	
	if(voto<0)
		printf("Eh? Così scarso?");
	/* Questo programma è identico a quello di poco fa, mi rifiuto di continuare a scrivere. */
	else
		printf("Oh OH oh, abbiamo un genio qui!");
		
printf("\n");
}