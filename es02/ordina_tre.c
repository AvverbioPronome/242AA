#include <stdio.h>

int main(){
	int a, b, c;
	int max, med, min;
	
	if (a>b && a > c){
		max=a;
		if (b>c){
			med=b;
			min=c;
		}else{
			med=c;
			min=b
		}
	}else{
		if (b>c){
			max=b;
			if (a>c)
				
		}
		else max=c;
	}
	

}